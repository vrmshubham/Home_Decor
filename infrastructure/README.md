# Infrastructure

Deployment and container configuration will be added after the frontend and backend can run locally.

The first deployment should keep frontend, backend, PostgreSQL and product media as separate services.
